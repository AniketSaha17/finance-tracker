services:
  - type: web
    name: finance-tracker-backend
    env: python
    branch: main                 # Make sure this is your GitHub branch
    root: backend                # This tells Render where to run commands
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port 10000
    plan: free
    envVars:
      - key: SECRET_KEY
        value: MLkJPEyzpAQnU6y7
